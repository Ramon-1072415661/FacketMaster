<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.facketmaster</groupId>
    <artifactId>facketmaster-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>FacketMaster - Parent</name>
    <description>Monorepo pai do sistema distribuído de ingressos FacketMaster</description>

    <modules>
        <module>common</module>
        <module>event-service</module>
        <module>payment-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Versões centralizadas — atualize aqui para atualizar todo o projeto -->
        <!-- Spring Boot 3.5.x: linha ativa com suporte OSS até jun/2026         -->
        <spring-boot.version>3.5.14</spring-boot.version>

        <!-- jjwt 0.12.x: API estável, requer JwtUtil atualizado (ver abaixo)    -->
        <jjwt.version>0.12.6</jjwt.version>

        <!-- springdoc 2.8.x: compatível com Spring Boot 3.x                     -->
        <!-- springdoc 3.x requer Spring Boot 4 — não usar ainda                 -->
        <springdoc.version>2.8.5</springdoc.version>

        <!-- Plugins -->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>

        <!--
            Lombok: versão explícita necessária para o annotationProcessorPaths.
            O Spring Boot BOM gerencia o lombok no classpath de compilação,
            mas o annotationProcessorPaths precisa da versão declarada aqui.
        -->
        <lombok.version>1.18.36</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM — gerencia todas as versões de dependências Spring -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Swagger / OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>com.auth0</groupId>
                <artifactId>java-jwt</artifactId>
                <version>4.4.0</version>
                <scope>compile</scope>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${java.version}</release>
                        <!--
                            Necessário para springdoc + Spring Boot 3.2+:
                            preserva nomes de parâmetros sem @ParameterName.
                        -->
                        <parameters>true</parameters>
                        <!--
                            Declara o Lombok explicitamente como annotation processor.
                            Obrigatório quando <parameters>true</parameters> está ativo:
                            o compilador descarta processors implícitos do classpath e
                            exige a lista explícita, fazendo @Slf4j/@Data/@Builder pararem
                            de funcionar sem esta configuração.
                        -->
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>