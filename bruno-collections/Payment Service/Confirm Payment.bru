meta {
  name: Confirm Payment
  type: http
  seq: 4
}

post {
  url: {{base_url_payment_service}}/api/v1/pedidos/{{order_id}}/confirmar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

