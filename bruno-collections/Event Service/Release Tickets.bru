meta {
  name: Release Tickets
  type: http
  seq: 10
}

post {
  url: {{base_url_event_service}}/api/v1/eventos/{{event_id}}/liberar
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
    "quantidade": 2
  }
}

