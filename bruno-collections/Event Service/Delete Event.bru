meta {
  name: Delete Event
  type: http
  seq: 7
}

delete {
  url: {{base_url_event_service}}/api/v1/eventos/admin/{{event_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}
