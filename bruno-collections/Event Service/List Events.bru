meta {
  name: List Events
  type: http
  seq: 2
}

get {
  url: {{base_url_event_service}}/api/v1/eventos
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

params {
  nome:
  apenasDisponiveis: false
}
